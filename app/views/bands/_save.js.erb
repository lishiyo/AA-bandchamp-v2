$("ul.errors").html("");

<% if @band.errors.any? %>
  <% @band.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
    <% end %>
<% else %>
 
  $(".new-band").html("<%= escape_javascript(render partial: 'band_preview', locals: { band: @band }) %>")
  
  $("#new-band-modal").modal("hide")
<% end %>
